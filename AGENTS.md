<!-- gittensory-repo-doc:start -->
# AGENTS.md

This file is generated by [Gittensory](https://gittensory.aethereal.dev) from a profile of this repository's own
code -- it is not hand-written and not a generic template. Content between the markers above and below this
line is recomputed on every refresh; add anything you want kept forever outside them instead.

## Architecture

899 indexed source files across 11 top-level directories:

- `apps` -- 410 files
- `tests` -- 267 files
- `packages` -- 71 files
- `scripts` -- 49 files
- `integrations` -- 34 files
- `.` -- 21 files
- `.github` -- 21 files
- `docs` -- 20 files
- `.trunk` -- 4 files
- `.devcontainer` -- 1 file
- `cloudflare` -- 1 file

## Conventions

- File naming: kebab-case (`my-file.ts`)
- Test files: a `tests/`/`__tests__/` directory

## Commands

- Package manager: pnpm
- Build: `pnpm run build`, `pnpm run validate:web-build-log`
- Test: `pnpm run test`, `pnpm run test:commercial-intake`, `pnpm run test:coverage`, `pnpm run test:mcp`, `pnpm run test:registry-artifacts`, `pnpm run test:seo-jsonld`, `pnpm run test:submission-pr-first`
- Lint: `pnpm run commitlint`, `pnpm run type-check`

## Contribution workflow

- CI publishes a required check: no
- Linked-issue policy: optional
- Requires a linked issue: no
- CI workflow files:

- `.github/workflows/content-validation.yml`
- `.github/workflows/coverage.yml`
- `.github/workflows/indexnow-on-publish.yml`
- `.github/workflows/jobs-source-revalidation.yml`
- `.github/workflows/mcp-package.yml`
- `.github/workflows/mcp-release-watch.yml`
- `.github/workflows/package-artifact-scan.yml`
- `.github/workflows/pipelock-security.yml`
- `.github/workflows/publish-mcp-npm.yml`
- `.github/workflows/publish-mcp-registry.yml`
- `.github/workflows/raycast-extension.yml`
- `.github/workflows/raycast-release-watch.yml`
- `.github/workflows/readme-refresh-pr.yml`
- `.github/workflows/release-please.yml`
- `.github/workflows/smithery-publish.yml`
- `.github/workflows/superagent-security.yml`

---

Generated by Gittensory from this repository's own indexed code.
<!-- gittensory-repo-doc:end -->
